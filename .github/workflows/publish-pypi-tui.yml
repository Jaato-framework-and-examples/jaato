name: "Publish jaato-tui to PyPI"

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e jaato-sdk/ && pip install -e "jaato-tui/[dev]"

      - name: Run tests
        run: pytest jaato-tui/tests/

  build-and-publish:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: pypi-tui
    steps:
      # fetch-depth: 0 — scripts/build_readme.py derives the changelog that
      # becomes the package's PyPI long description from `git log` since the
      # previous "Bump <pkg>" commit.  checkout's default depth of 1 leaves it
      # a single commit to look at, so the published changelog was whatever
      # that one commit happened to be.
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install build
        run: pip install build

      - name: Check version not already on PyPI
        run: |
          version=$(python -c "
          import tomllib, pathlib
          data = tomllib.loads(pathlib.Path('pyproject.toml').read_text())
          print(data['project']['version'])
          ")
          echo "Package version: $version"
          status=$(curl -s -o /dev/null -w '%{http_code}' \
            "https://pypi.org/pypi/jaato-tui/${version}/json")
          if [ "$status" = "200" ]; then
            echo "::error::Version ${version} of jaato-tui already exists on PyPI"
            exit 1
          fi
          echo "Version ${version} is available for publishing"
        working-directory: jaato-tui

      - name: Generate combined README
        run: python ../scripts/build_readme.py
        working-directory: jaato-tui

      - name: Build package
        run: python -m build .
        working-directory: jaato-tui

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: jaato-tui/dist/
