$ jaato-scaffold explain oversight screener
human oversight, as ARMED by profile 'screener':

  regulatory.risk_class     limited
  interacts_with_persons    True
  announcement              'You are interacting with an AI system operated by Acme Support GmbH, not with a human being.'
                            emitted once at session creation; a client
                            sending client_discloses_ai withholds it.
                            Either way an `announcement` record (text,
                            channel, locale, model) goes to the ledger
                            when trace.ledger is set -- explain audit

  DECIDE / OVERRIDE  NO permission policy declared -- the runtime policy of the root session applies (a subagent inherits it)
  STOP (budget)      limits {'turns': 2}; ladder [100%->abort]; STOPS the run
  STOP (watchdog)    max_orphan_seconds=900 (default)  max_session_seconds=unbounded
  COMPLETION GATE    none
  IRREVERSIBLE       no subprocess or remote surface enabled

  the two stop verbs apply to every profile alike -- `explain oversight` (bare) names them.
