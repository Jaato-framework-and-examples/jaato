$ jaato-scaffold validate /srv/acme-support
[info] [workspace] reader: echo_is_a_test_double: provider 'echo' is the framework's deterministic test double — no credentials, no network, fixed responses.  It is absent from `explain providers` (nobody should pick it for production) but it IS installed, and it is what the live conformance suite runs on. @ provider
[warn] [workspace] reader: budget_control_absent: declares no budget_control, so this session is unbounded on every dimension (usd, tokens, seconds, tool_calls, turns) — nothing stops a tool-call loop, and the first sign is the provider bill. Declare a ceiling AND a rung that enforces it, e.g. budget_control: {limits: {tool_calls: 200, usd: 5.0}, degrade: [{at: 100, action: abort}]}. @ budget_control
[info] [workspace] screener: echo_is_a_test_double: provider 'echo' is the framework's deterministic test double — no credentials, no network, fixed responses.  It is absent from `explain providers` (nobody should pick it for production) but it IS installed, and it is what the live conformance suite runs on. @ provider
[info] [workspace] writer: echo_is_a_test_double: provider 'echo' is the framework's deterministic test double — no credentials, no network, fixed responses.  It is absent from `explain providers` (nobody should pick it for production) but it IS installed, and it is what the live conformance suite runs on. @ provider
[warn] [workspace] writer: budget_control_absent: declares no budget_control, so this session is unbounded on every dimension (usd, tokens, seconds, tool_calls, turns) — nothing stops a tool-call loop, and the first sign is the provider bill. Declare a ceiling AND a rung that enforces it, e.g. budget_control: {limits: {tool_calls: 200, usd: 5.0}, degrade: [{at: 100, action: abort}]}. @ budget_control
[warn] [workspace] reader: require_curation_without_curator: plugin_configs.memory.require_curation is on and no profile in this workspace binds a curator persona — every stored memory is withheld from retrieval, so the knob currently means 'never re-inject anything'. Article 15(4) asks that a learning loop be MITIGATED, not closed: add a curator agent that promotes raw memories (docs/design/agent-continuity.md), or turn the knob off. @ plugin_configs.memory.require_curation
