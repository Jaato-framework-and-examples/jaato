# Plugin schema census

Generated by `python scripts/plugin_schema_census.py`. **Not a guard** — see that script's docstring for the four surfaces this counts across and why a ratchet needs them separated first.

- plugins scanned: **45**
- plugins with at least one undeclared read: **26**
- framework-injected reads (surface 1, mechanically separable): **33**
- unclassified reads (surfaces 2/3/4, need a decision): **90**

Framework-injected keys, read from `PluginRegistry._augment_plugin_config`: `agent_name`, `config_root`, `session_id`, `workspace_path`

| plugin | declared | framework-injected | unclassified |
|---|---:|---|---|
| `todo` | 1 | `agent_name` | `_injected_reporter`, `auth_token`, `base_path`, `callback`, `colors`, `compact`, `config_path`, `display`, `endpoint`, `headers`, `max_events`, `output_callback`, `output_func`, `progress_bar`, `reporter`, `reporter_config`, `reporter_type`, `show_timestamps`, `storage`, `storage_path`, `storage_use_directory`, `timeout`, `version`, `width` |
| `references` | 6 | `agent_name`, `workspace_path` | `auth_token`, `base_path`, `channel`, `channel_config`, `channel_type`, `config_path`, `embedding_model`, `endpoint`, `exclude_tools`, `headers`, `input_func`, `output_func`, `poll_interval`, `skip_readline_history`, `sources`, `timeout`, `version` |
| `permission` | 28 | — | `auth_token`, `base_path`, `channel`, `endpoint`, `headers`, `input_func`, `output_func`, `poll_interval`, `skip_readline_history`, `timeout`, `use_colors`, `version` |
| `filesystem_query` | 1 | — | `context_lines`, `exclude_mode`, `exclude_patterns`, `include_patterns`, `max_file_size_kb`, `max_results`, `timeout_seconds`, `version`, `workspace_root` |
| `notebook` | 8 | `agent_name` | `access_token`, `api_token`, `backend`, `token`, `workspace_root` |
| `file_edit` | 0 | `agent_name`, `config_root`, `session_id` | `allow_full_replace`, `backup_dir`, `max_edit_span_chars`, `workspace_root` |
| `artifact_tracker` | 0 | `agent_name` | `auto_load`, `storage_path`, `workspace_root` |
| `ast_search` | 3 | — | `allow_tmp`, `workspace_root` |
| `clarification` | 0 | `agent_name` | `channel_config`, `channel_type` |
| `sandbox_manager` | 0 | `session_id` | `allowed_paths`, `denied_paths` |
| `waypoint` | 0 | `session_id` | `backup_manager`, `storage_path` |
| `cli` | 6 | `agent_name` | `workspace_root` |
| `interactive_shell` | 7 | `agent_name` | `workspace_root` |
| `memory` | 4 | `agent_name`, `session_id` | `global_storage_path` |
| `multimodal` | 1 | `agent_name` | `base_path` |
| `subagent` | 4 | `agent_name` | `scrub_secret_env` |
| `template` | 1 | `agent_name` | `base_path` |
| `thinking` | 0 | — | `config_path` |
| `web_fetch` | 10 | `agent_name` | `user_agent` |
| `background` | 0 | `agent_name` | — |
| `calculator` | 1 | `agent_name` | — |
| `lsp` | 7 | `agent_name`, `session_id`, `workspace_path` | — |
| `mcp` | 2 | `agent_name`, `session_id`, `workspace_path` | — |
| `prompt_library` | 0 | `agent_name`, `workspace_path` | — |
| `service_connector` | 0 | `agent_name`, `config_root`, `workspace_path` | — |
| `web_search` | 4 | `agent_name` | — |

Declaring everything they read: `anthropic_auth`, `antigravity_auth`, `bundle`, `doubleword_auth`, `environment`, `github_auth`, `introspection`, `kimi_auth`, `mimo_auth`, `minimax_auth`, `nebius_auth`, `nim_auth`, `openrouter_auth`, `ovhcloud_auth`, `reliability`, `result_grep`, `telepathy`, `webhook`, `zhipuai_auth`.

## Worked example: `permission`

The only row audited site by site (jaato PR #1040). It started at 16 undeclared reads and is the evidence for the four surfaces above.

| was | surface | resolution |
|---|---|---|
| `agent_name`, `config_path`, `workspace_path`, `channel_type`, `channel_config` | 2 — the block an author writes | **declared**. Read straight off `initialize(config)` (`plugin.py:508-544`); an author configuring a webhook approval channel had no route to the shape but the source |
| `cwd`, `sanitization.custom_blocked_commands`, `path_scope.resolve_symlinks` | 2, nested | **declared**. Read by `PermissionPolicy.from_config` (`policy.py:646-668`) and reported as typos by the nested descent until they were |
| `endpoint`, `headers`, `auth_token`, `timeout`, `base_path`, `poll_interval` | 3 — a nested dict with its own owner | `channel_config` is marked `additionalProperties`. These names belong to the channel, and nothing in this plugin should judge them |
| `version`, `channel` | 4 — a different file | keys of the permissions JSON `config_path` names, whose shape `config_loader.validate_config` owns (*"Raw configuration dict loaded from JSON"*). Not `plugin_configs.permission` keys at all |
| `input_func`, `output_func`, `use_colors`, `skip_readline_history` | 3 — console channel | constructor arguments of the console channel, reached through `channel_config` |

Note what the fourth row costs a naive count: two of the sixteen were never this block's keys, so "undeclared" would have been wrong about them in a direction no amount of declaring could fix.
