# Completion-Payload Schema — Authoring Conventions

> **Moved.** This document was unified with the symmetric
> `spawn_payload_schema` conventions into a single guide:
>
> **→ [`payload-schema-conventions.md`](./payload-schema-conventions.md)**
>
> Existing rules (always carry `warnings[]` and `errors[]`,
> persona ↔ schema consistency, what to strip from canonical
> hashes in determinism tests) live unchanged in §3.2, §4, and §5
> of the unified doc. Spawn-side conventions and the
> `agent_params`-mediated coupling with [agent
> continuity](./agent-continuity.md) are covered in §3.1 and §6.

This stub stays in place so the old path keeps resolving. Update
new references to point at `payload-schema-conventions.md`.
