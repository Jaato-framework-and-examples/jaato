Use the gh CLI tool to list issues in {{OWNER}}/{{REPO}}.

Command: gh issue list --repo {{OWNER}}/{{REPO}} --limit {{LIMIT}} --json number,title,updatedAt,state

Compute age_days from NOW_UTC={{NOW_UTC}} and return JSON:
{"issues": [{"number": ..., "title": "...", "state": "...", "age_days": ...}]}
