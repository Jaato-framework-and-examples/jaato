Use the CLI Confluence tool as "confluence-cli content get {{PAGE_ID}}" to retrieve a page by its id.
After the tool output (fields: title, body), return JSON: {"title":..., "summary":..., "key_points":[...]}
Summary: 3 sentences. Key points: <=6.
Avoid adding content not present.
