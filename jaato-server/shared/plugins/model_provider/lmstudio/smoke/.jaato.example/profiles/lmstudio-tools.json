{
  "name": "lmstudio-tools",
  "description": "Tool-calling smoke test for the lmstudio provider. Exercises the OpenAI tools shape via the cli plugin — one tool call followed by a one-sentence summary. Permission policy is default-allow so the harness needs no interactive permission handling.",

  "model": "openai/gpt-oss-20b",
  "provider": "lmstudio",

  "max_turns": 4,

  "plugins": ["cli"],

  "plugin_configs": {
    "lmstudio": {
      "host": "${LMSTUDIO_HOST}"
    },
    "permission": {
      "policy": {
        "defaultPolicy": "allow"
      }
    }
  }
}
