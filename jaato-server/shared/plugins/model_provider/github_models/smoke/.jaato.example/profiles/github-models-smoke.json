{
  "name": "github-models-smoke",
  "description": "Pure chat smoke test for the github_models provider against the managed GitHub Models endpoint. No tools, no GC — just round-trips /chat/completions to validate the provider wire.",

  "model": "openai/gpt-4o",
  "provider": "github_models",

  "max_turns": 1,

  "plugins": [],

  "plugin_configs": {
    "github_models": {
      "api_key": "${GITHUB_TOKEN}"
    }
  }
}
