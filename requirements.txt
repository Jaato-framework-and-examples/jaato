click
requests
google-genai
mcp[cli]
google-api-core
google-auth
certifi
python-dotenv
pyreadline3
prompt_toolkit>=3.0.0
rich>=13.0.0
reportlab
ddgs>=7.0.0  # provides web search via DuckDuckGo
pyyaml
pexpect>=4.8.0  # PTY-based interactive shell sessions
pyte>=0.8.0  # Terminal emulator for processing tool output control sequences
azure-ai-inference>=1.0.0b7  # GitHub Models API via Azure AI Inference SDK
websockets>=12.0  # WebSocket server for headless mode
anthropic>=0.40.0  # Anthropic Claude API
pybars3>=0.9.7  # Handlebars template rendering (supports {{#if}}, {{#each}}, etc.)

# web_fetch plugin dependencies
httpx>=0.27.0  # Modern HTTP client (recommended over requests for web_fetch)
trafilatura>=1.6.0  # Content extraction for articles/docs (removes boilerplate)
html2text>=2024.2.26  # HTML to markdown conversion (fallback)
beautifulsoup4>=4.12.0  # HTML parsing for structured extraction
lxml>=5.0.0  # Fast HTML parser backend for BeautifulSoup

# Notebook plugin dependencies
ipython-pygments-lexers>=1.0.0  # IPython syntax highlighting (handles !shell, %magic)

# AST search plugin dependencies (optional)
ast-grep-py>=0.30.0  # AST-based structural code search

# Kerberos proxy authentication (optional)
pyspnego>=0.10.0  # SPNEGO/Negotiate auth for corporate proxies (Windows SSPI, macOS GSS, Linux Kerberos)
